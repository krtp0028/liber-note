export const WELCOME_MD = `# Liber

Lightweight hierarchical Markdown notes. Your notes stay plain \`.md\` files in a folder you own — no account, no sync service, no lock-in.

> In every walk with nature one receives far more than he seeks.
>
> — John Muir

## Start here

1. **Open a folder of Markdown files** — \`Ctrl+O\`. Any folder works, even one that already lives in your repo.
2. **Build the tree with frontmatter** — \`Ctrl+N\` for a note, then add \`parent:\`, \`also_under:\` or \`tags:\` to shape how it appears.
3. **Drop in code** — \`Ctrl+Shift+E\` inserts a fenced block and writes the detected language into it.

\`Ctrl+Shift+P\` opens the command palette, \`Ctrl+Shift+V\` cycles the preview, and **Run vault doctor** audits the vault and offers previewed repairs.
`;
